syntax = "proto3";
package protocol_go;

option go_package = "./protocol_go";

// 基础返回体 具体信息会在message里面
message StandardRsp {
  int32  status  = 1; // 返回状态码
  string error   = 2; // 错误信息
  string message = 3; // 返回信息
}